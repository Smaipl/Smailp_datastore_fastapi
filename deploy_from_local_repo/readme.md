### Особенности деплоя из образов

* скопировать в корень файлы и каталоги: grafana/, .env (подправить), Caddyfile (подправить), entrypoint.sh, migrations/
* поменять логин и пароль для пользователеля read_user (в третьей миграции, где он создаётся)
* залогиниться в репозиторий (или скопировать креды)
* запустить docker-compose up -d

